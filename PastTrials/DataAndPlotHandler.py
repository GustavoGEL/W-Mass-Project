import matplotlib.pyplot as plt
import numpy as np

# Program that handles changing between Python and C

# 1) Check if C program exists and if it is already compiled
# 2) Check if input data exists. If so, run C program with that data. Expect an output program with all the values
# 3) Wait for C program to finish and check for output file. Once it is done, check if Python plotting script exists
# 4) If exists, read output data from C and run Python plotter
# 5) When done, open Output image
