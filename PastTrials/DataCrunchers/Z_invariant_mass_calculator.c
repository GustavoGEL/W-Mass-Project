/* Optimised version to run as fast as possible, using Linux to compile? */
#include <stdio.h>

int main() {
    int test_num = 195;
    printf("Hello, working and number is %d\n", test_num);

    /* To develop for program 
    1) Need a function to store all the output data in a file
    2) Need a function for each of the steps of the calculations
    3) Need an initializer which can read data from a file, i.e. a .txt file created with python from the read events? */
}
